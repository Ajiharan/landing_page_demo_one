import React from "react";
import Cards from "../Cards";
import HeadSection from "../HeadSection";

const Home = () => {
  return (
    <React.Fragment>
      <HeadSection />
      <Cards />
    </React.Fragment>
  );
};

export default Home;
